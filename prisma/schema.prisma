datasource db {
  provider          = "postgresql"
  url               = env("DATABASE_URL")
  directUrl         = env("DIRECT_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model BeneficiaryUser {
  id               String             @id @default(cuid())
  cpf              String             @unique
  password         String
  name             String?
  type_beneficiary String?
  enrollment       Int    
  createdAt        DateTime           @default(now())
  updatedAt        DateTime           @updatedAt
  pdfFiles         BeneficiaryPdfFile[]
}

model BeneficiaryPdfFile {
  id            String  @id @default(cuid())
  cpf           String
  fileName      String
  fileType      PdfFileType
  file          Bytes[]
  year          Int?
  month         Int?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  beneficiary   BeneficiaryUser?    @relation(fields: [cpf], references: [cpf])
}

enum PdfFileType {
  HOLERITE
  DESMOTRATIVO_ANUAL
}

model AdminUser {
  id        String    @id @default(cuid())
  email     String    @unique
  password  String
  name      String
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}